import React from 'react'
import './styles.sass'
import {IGameRoom} from '../models'

export const RoomCard = ({title, description, currentPlayers, maxPlayers, image}: IGameRoom) => {
    return <div className="d-flex room-card">

    </div>
}